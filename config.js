// Marlo (seekmarlo.com) — connection settings
//
// Fill these two lines in from your Supabase project:
//   Supabase dashboard -> your project -> Project Settings (gear icon) -> API
//   SUPABASE_URL       = the "Project URL"        (looks like https://abcdefgh.supabase.co)
//   SUPABASE_ANON_KEY  = the "anon" "public" key   (a long string of letters/numbers)
//
// The anon key is SAFE to put here and to publish publicly — it's the
// public-facing key, and the database rules (in schema.sql) control exactly
// what it's allowed to do. Never put your other key ("service_role") anywhere
// in this website's files — that one bypasses all the safety rules.

const SUPABASE_URL = "PASTE_YOUR_SUPABASE_PROJECT_URL_HERE";
const SUPABASE_ANON_KEY = "PASTE_YOUR_SUPABASE_ANON_KEY_HERE";
